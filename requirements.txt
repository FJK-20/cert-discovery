fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
httpx>=0.27,<1.0
cryptography>=50,<51
dnspython>=2.6,<3.0
qrcode>=7.4,<8.0
acme>=3.0,<6.0
# pyopenssl é dependência transitiva do acme (que só exige >=25.0.0) — sem
# pin explícito aqui, um install do zero (ex: CI, deploy novo) reinstala a
# versão vulnerável de novo mesmo com cryptography corrigido acima
# (achados PYSEC-2026-2268/2269, corrigidos a partir da 26.0.0).
pyopenssl>=26.0,<27
# SSO via SAML 2.0 (Entra ID / Azure AD) — assinatura e validação de XML é
# segurança crítica o bastante pra não reimplementar na mão (mesmo
# raciocínio já aplicado à lib `acme` acima): usa a lib de referência da
# OneLogin em vez de parsear XML assinado com as próprias mãos.
python3-saml>=1.16,<2.0
# Starlette's Request.form() (usado em app/auth/saml.py pra ler o
# SAMLResponse do POST binding) precisa dessa lib pra parsear
# application/x-www-form-urlencoded — sem ela, request.form() lança
# AssertionError em runtime (não falha em import, só no primeiro POST real,
# por isso passou despercebido pelos testes até uma tentativa real de login).
python-multipart>=0.0.12,<0.1
